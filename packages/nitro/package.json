{
  "private": true,
  "name": "nitro",
  "version": "1.0.0",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node --env-file=.env .output/server/index.mjs",
    "postinstall": "nitro prepare && nitro build"
  },
  "devDependencies": {
    "nitropack": "latest"
  },
  "dependencies": {
    "@prisma/client": "^5.15.1"
  }
}
